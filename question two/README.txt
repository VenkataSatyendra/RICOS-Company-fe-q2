How to Run the code:
download the following extenions mentioned below:
       Code Runner(Jun Han)
       JavaScripts(ES6)
       Live Server(Ritwick Dey)
       open in browser(TechER)
       Prettier-Code formatter
       run at broswer(optional)

once all the extensions got downloaded open the questiontwo.html 
in new default broswer 
select the 3D image want to test and click on load model button 
then the following model will be downloaded into the testing system 
then open file and can interact with the 3D model .

the following application is a responsive design can use in any kind of 
device screen 
